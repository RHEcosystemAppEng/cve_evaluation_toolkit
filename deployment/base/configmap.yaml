apiVersion: v1
kind: ConfigMap
metadata:
  name: evaluation-config
data:
  # API Configuration (non-sensitive)
  EXPLOIT_IQ_API_BASE: "TO_BE_REPLACED"

  # LLM Judge Configuration
  JUDGE_MODEL: "google/gemma-4-26B-A4B-it"
  JUDGE_BASE_URL: "TO_BE_REPLACED"

  # Evaluation Settings
  EVALUATION_TIMEOUT: "300"

  # Logging
  JUDGE_TEMPERATURE: "0.2"
  LOG_LEVEL: "INFO"
